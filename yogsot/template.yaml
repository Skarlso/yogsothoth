Parameters:
  StackName:
    Description: The name of the stack to deploy
    Type: String
    Default: FurnaceStack
  Port:
    Description: Test port
    Type: Number
    Default: 80

Resources:
  # The name of the resource
  Droplet:
    Name: MyDroplet
    # Type of the resource which will be used to determine what to parse it as
    Type: Droplet
